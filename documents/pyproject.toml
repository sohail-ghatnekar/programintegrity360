[project]
name = "pi360-synthetic-documents"
version = "0.1.0"
description = "Deterministic synthetic PDF fixtures for the Program Integrity 360 demo"
requires-python = ">=3.11"
dependencies = [
    "pypdf>=6,<7",
    "reportlab>=4,<5",
]

[dependency-groups]
dev = [
    "pytest>=9,<10",
]
