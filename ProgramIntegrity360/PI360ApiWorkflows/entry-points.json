{
  "$schema": "https://cloud.uipath.com/draft/2024-12/entry-point",
  "entryPoints": [
    {
      "filePath": "Main.json",
      "uniqueId": "d525fb99-f53c-4fd9-bf44-99988c748d77",
      "displayName": "Program Integrity 360 Case Lifecycle API Contracts",
      "type": "api",
      "input": {
        "type": "object",
        "properties": {
          "workflowName": { "type": "string", "description": "Simplified PI360 stage automation contract name." },
          "caseType": { "type": "string", "description": "MedicaidPCS or StateMedicaidHospice." },
          "caseId": { "type": "string", "description": "Program Integrity case identifier." },
          "providerId": { "type": "string", "description": "Billing or servicing provider identifier." },
          "claimTotalBilled": { "type": "number", "description": "Claim aggregate used by deterministic threshold logic." },
          "hospitalRecordAvailable": { "type": "boolean", "description": "Whether the requested hospital record has returned." },
          "investigatorProceed": { "type": "boolean", "description": "Human investigator gate for the provider record request." },
          "supervisorApproved": { "type": "boolean", "description": "Human supervisor gate for closure and communication." },
          "supervisorDecision": { "type": "string", "description": "Final supervisor disposition selected in the human task." },
          "supervisorReviewNotes": { "type": "string", "description": "Supervisor rationale persisted with the final decision." },
          "investigatorFindings": { "type": "string", "description": "Investigator findings preserved in the closure decision." },
          "recommendedAction": { "type": "string", "description": "Human investigator recommendation reviewed by the supervisor." },
          "requesterEmail": { "type": "string", "description": "Case requester or logged-in user associated with the audit write." },
          "caseInput": { "type": "object", "additionalProperties": true, "description": "Case trigger details, including CaseType, case ID, requester email, and optional Maestro instance ID." },
          "claimInput": { "type": "object", "additionalProperties": true, "description": "Claim trigger details used to initialize the case header." },
          "memberInput": { "type": "object", "additionalProperties": true, "description": "Member trigger details used to initialize the case header." },
          "providerInput": { "type": "object", "additionalProperties": true, "description": "Provider trigger details used to initialize the case header." },
          "serviceEventInput": { "type": "object", "additionalProperties": true, "description": "Service-event trigger details used to initialize the case header." },
          "documentInput": { "type": "object", "additionalProperties": true, "description": "Document trigger details used to initialize the case header." }
        },
        "required": []
      },
      "output": {
        "type": "object",
        "properties": {
          "workflowName": { "type": "string" },
          "caseType": { "type": "string" },
          "caseId": { "type": "string" },
          "status": { "type": "string" },
          "gated": { "type": "boolean" },
          "recordsAffected": { "type": "number" },
          "persistedCaseId": { "type": "string" },
          "registrationMode": { "type": "string" },
          "message": { "type": "string" },
          "payload": { "type": "object" }
        },
        "required": []
      }
    }
  ]
}
