[project]
name = "PI360QuickRulesCodedAgent"
version = "0.0.1"
description = "Program Integrity 360 deterministic quick-rules coded agent"
authors = [{ name = "Sohail Ghatnekar", email = "sohail.ghatnekar@uipath.com" }]
dependencies = [
    "uipath-langchain>=0.15.0, <0.16.0",
]
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "uipath-dev",
]
